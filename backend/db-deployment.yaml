apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolo-db-deployment
spec:
  selector:
    matchLabels:
      app: yolo-db-deployment
  replicas: 1
  template:
    metadata:
      labels:
        app: yolo-db-deployment
    spec:
      containers:
        - name: yolo-db-deployment
          image: mongo:4.0.17
          ports:
            - containerPort: 27017
          
